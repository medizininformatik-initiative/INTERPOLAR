% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{convertListColumnsToString}
\alias{convertListColumnsToString}
\title{Convert list or vector columns to character in a data.table}
\usage{
convertListColumnsToString(dt, separator = " ~ ")
}
\arguments{
\item{dt}{The input data.table.}

\item{separator}{The separator to use when concatenating list elements (default is " ~ ").}
}
\value{
Returns the modified data.table with list columns converted to character columns.
}
\description{
This function takes a data.table and converts all list-type columns into
character columns by concatenating the list elements with a specified separator.
}
\examples{
library(data.table)

# Create example data
dt <- data.table(
  ID = 1:3,
  Names = list(c("Alice", "Bob"), c("Charlie", "David"), c("Eve", "Frank")),
  Scores = list(c(85, 92), c(78, 89), c(95, 88))
)
print(dt)

# Convert list columns to character columns
dt <- convertListColumnsToString(dt)
print(dt)

}
