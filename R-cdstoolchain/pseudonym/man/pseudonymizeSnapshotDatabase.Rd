% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudonymize_snapshot_db.R
\name{pseudonymizeSnapshotDatabase}
\alias{pseudonymizeSnapshotDatabase}
\title{Pseudonymize a Snapshot from Source DB to Target DB Connections}
\usage{
pseudonymizeSnapshotDatabase(
  source_connection,
  target_connection,
  table_descriptions = NULL,
  snapshot_extensions = NULL,
  project_root = ".",
  input_repo_path = NULL,
  source_schema = NULL,
  target_table_schema = "db_log",
  target_view_schema = "db2dataprocessor_out",
  source_view_prefix = "v_",
  last_version_suffix = "_last_version",
  tables = NULL,
  enrich_tables = NULL,
  fail_on_review_problems = TRUE,
  write_review_report = TRUE,
  review_report_file = NA,
  enrichment_review_report_file = NA,
  keep_unmatched_columns = FALSE,
  overwrite_tables = FALSE,
  replace_views = FALSE,
  temporary = FALSE,
  log_steps = TRUE
)
}
\arguments{
\item{source_connection}{Source DBI connection.}

\item{target_connection}{Target DBI connection.}

\item{table_descriptions}{Rule source specification. If \code{NULL}, the default
INTERPOLAR snapshot rule sources from \code{project_root} are used.}

\item{snapshot_extensions}{Optional snapshot-extension rule source
specification. Ignored when \code{table_descriptions} is \code{NULL} and defaults are
used.}

\item{project_root}{Repository root used for default rule sources.}

\item{input_repo_path}{TOML-configured input repository directory used for
\code{pseudonym(sheet = ...)} mapping rules.}

\item{source_schema}{Optional schema containing source views.}

\item{target_table_schema}{Target schema for materialized pseudonymized tables.}

\item{target_view_schema}{Target schema for passthrough analysis views.}

\item{source_view_prefix}{Prefix used for source view names.}

\item{last_version_suffix}{Suffix used for last-version source views and
materialized target tables.}

\item{tables}{Optional character vector limiting tables to read.}

\item{enrich_tables}{Optional function called with the named source-table
list before pseudonymization. It must return a named table list.}

\item{fail_on_review_problems}{Passed to \code{pseudonymizeSnapshotTables()}.}

\item{write_review_report}{Passed to \code{pseudonymizeSnapshotTables()}.}

\item{review_report_file}{Passed to \code{pseudonymizeSnapshotTables()}.}

\item{enrichment_review_report_file}{Optional explicit enrichment review
report path. If \code{NA}, the report is written to
\verb{outputLocal/<MODULE>/reports}.}

\item{keep_unmatched_columns}{Passed to \code{pseudonymizeSnapshotTables()}.}

\item{overwrite_tables}{Passed to \code{writeSnapshotTargetTables()}.}

\item{replace_views}{Passed to \code{createSnapshotPassthroughViews()}.}

\item{temporary}{Passed to \code{writeSnapshotTargetTables()}.}

\item{log_steps}{If \code{TRUE} and module logging is initialized, wrap major
steps in the existing \code{etlutils::runLevel...} logging.}
}
\value{
A list with source tables, pseudonymization result, and write summary.
}
\description{
This is the DBI-based entry point for the snapshot pseudonymization core. It
loads rule sources, reads \verb{v_<table>} and \verb{v_<table>_last_version} from the
source connection, materializes pseudonymized tables in \code{db_log}, and creates
passthrough views in \code{db2dataprocessor_out}. Opening the source/target
databases from site-specific TOML files remains a caller responsibility.
}
