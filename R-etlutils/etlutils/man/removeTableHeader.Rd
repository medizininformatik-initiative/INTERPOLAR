% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lib_table.R
\name{removeTableHeader}
\alias{removeTableHeader}
\title{Remove the table header from a data table.}
\usage{
removeTableHeader(dt, pattern_list)
}
\arguments{
\item{dt}{A data table with a header that needs to be removed.}

\item{pattern_list}{A list of patterns to identify the row with column names.}
}
\value{
The data table with the header removed and updated column names.
}
\description{
This function removes the rows above the row containing column names
that match the specified patterns. It then updates the column names
in the data table.
}
\examples{
library(data.table)

# Create a sample data table with a header
dt <- data.table(
  X1 = c("Table XYZ", "", "Name", "John", "Alice", "Bob"),
  X2 = c("", "", "Age", "25", "30", "22"),
  X3 = c("", "", "Country", "USA", "Canada", "UK")
)
print(dt)

# Define patterns for the header row
header_patterns <- c("Name", "Age", "Country")

# Remove the header and update column names
dt <- removeTableHeader(dt, header_patterns)
print(dt)

}
