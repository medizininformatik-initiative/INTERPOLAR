# Mapping workbook packaged in
# R-Fallvignette_Process_Evaluation/inst/extdata.
FALLVIGNETTE_MAPPING_FILE_NAME = "WP8MRP_Liste_Daten_Mapping20260722.xlsx"

# CSV and XLSX are written below outputGlobal/dataprocessor/reports.
FALLVIGNETTE_OUTPUT_FILE_NAME = "WP8_Fallvignetten_Import"

# Local-only crosswalk written below outputLocal/dataprocessor/data.
# Do not transfer this workbook with the REDCap import.
FALLVIGNETTE_ID_MAPPING_FILE_NAME = "Fallvignette_Process_Evaluation_ID_Mapping"

# Database credentials are loaded from this separate DB config. The
# DB_ANALYSIS_* values in that file must point to the pseudonymized database.
PATH_TO_DB_CONFIG_TOML = "./cds_hub_db_config.toml"
